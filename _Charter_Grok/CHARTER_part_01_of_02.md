<!-- CHARTER (Grok-readable split) - Part 1 of 2 - source: CLAUDE.md -->
<!-- LOSSLESS split of the workspace charter for 10k-char-limited readers (e.g. Grok Build).
     Read all 2 parts in order. GENERATED by split_charter.py - DO NOT EDIT here;
     edit the source CLAUDE.md and re-run `py split_charter.py`. -->

# CLAUDE.md — RV Park Website (Project Workspace)

> **CHARTER — note for Grok / 10k-char readers:** This is the workspace **charter**. If your reader
> truncates near 10,000 characters (e.g. Grok Build), read the lossless split in **`_Charter_Grok\`**
> instead — the full charter spliced into ≤10,000-character parts (`CHARTER_part_01_of_NN.md` …),
> regenerated by the root `split_charter.py`. This file itself is never truncated; the split is a
> Grok-readable mirror.

**Last updated:** 2026-07-19 PT (V3 production remodel, WYSIWYG retirement, protected recovery, and Netlify AI editing model).
**Project status:** Active development. Public site is live at https://www.crookedriverranchrv.com. The approved V3 production remodel is complete on `codex/v3-production-remodel` and awaits Deploy Preview review before merge to `main`. The admin is an operational console; public-page edits use Netlify Agent Runners with Claude Code or Codex.
**Local git repository:** Initialized — full history present. Remote: `https://github.com/CRRrvPark/CRR-RV-Park-Website` (branch `main`). Owner-on-record rule: **Claude owns git on this project** (commit + push per logical chunk, no asking — recorded in §4 *Iron-forged rules* below).

---

## How to use this file — read this first

This is the project-level instruction file for the **RV Park Website** project. Before doing any work in this workspace, also read the master workspace instruction file at:

> `C:\Claude Workspace\CLAUDE.md`

Claude Code should auto-load the master (it sits in a parent directory), but read it explicitly regardless — it holds the universal rules that apply to every project. This file does not repeat them; it elaborates on them for the website.

**Authority and conflict resolution.** This project file governs project-specific matters and elaborates on the master CLAUDE.md. Where the two do not conflict, they operate together. This file may specialize or extend the master, but it does **not** silently override it. The one explicit deviation from the master — TypeScript/Astro stack vs. the "Python exclusively" Code rule — is documented in §4 *Stack exception* and was an inherited reality (the project was built before being brought into the workspace).

---

## 1. Document Index

Workspace root: `C:\Claude Workspace\RV Park Website`

**Workspace documents (this charter's scope):**
- `CLAUDE.md` — this file. Project charter; subordinate to the master `C:\Claude Workspace\CLAUDE.md`.
- `FILE_LOG.md` — workspace activity log (file/folder additions, archives, moves).
- `PORT_TO_NEW_COMPUTER.md` — playbook for moving this project to a different machine without losing context.
- `_Charter_Grok\` — generated Grok 10k-character split of this charter (regenerated by root `split_charter.py`).
- `_Archive\` — phased-out workspace files. Contains the original 1-line scaffold `CLAUDE.md` (archived 2026-04-23).

**Inherited project documents (in the git repo — port automatically on clone):**
- `README.md` — top-level project intro.
- `RUNBOOK.md` — operational runbook (deploy, env vars, troubleshooting).
- `SETUP.md` — local-dev setup instructions.
- `PROJECT-DETAILS.md` — comprehensive project overview, decisions, history (the long-form companion to this charter).
- `NETLIFY-DEPLOY.md` — Netlify-specific deploy notes.
- `AREA-GUIDE-SETUP.md` — area-guide table setup notes (Google Places integration scoping).
- `HANDOFF-V*.md` — historical handoff documents (V1 → V4); V4 is the live initiative reference.
- `CHECKIN.md` — running-status check-in doc.
- `PATCHES-APPLIED.md` — patch history.
- `SECURITY-*.md`, `PEN-TEST-REPORT.md` — security audit + plan.
- `SPEC-PHASE-1.md` — phase 1 scope spec.

**Project code (workspace root):**
- `src/` — Astro app (pages, components, layouts, styles, lib).
- `public/` — static assets served at site root.
- `supabase/migrations/` — SQL migrations (017+ active; runner at `scripts/supabase-migrate.mjs`).
- `scripts/` — Node ESM utility scripts for content backfills, image uploads, photo proxies. Created in the past week of work: `backfill-photos.mjs`, `backfill-trail-photos.mjs`, `backfill-trail-static-maps.mjs`, `backfill-trails.mjs`, `cleanup-things-to-do.mjs`, `enrich-experience-adventures.mjs`, `enrich-fishing-watersports.mjs`, `enrich-site-descriptions.mjs`, `enrich-things-to-do.mjs`, `fix-amenities-page.mjs`, `fix-amp-encoding.mjs`, `fix-area-guide-photos.mjs`, `fix-area-guide-quality.mjs`, `fix-bad-images.mjs`, `rewrite-site-descriptions.mjs`, `upload-site-photos.mjs`, `add-area-guide-experiences.mjs`, `add-faith-hope-charity.mjs`.
- `package.json`, `astro.config.mjs`, `netlify.toml`, `tsconfig.json` — build config.
- `.env` — secrets (NOT in git, gitignored). Listed in §5 *Environment* and `PORT_TO_NEW_COMPUTER.md`.
- `.env.example` — template with names but no values; in git.

---

## 2. People & Roles

Mathew — see master CLAUDE.md §2.

No additional project-specific people recorded yet.

---

## 3. Project Intake (answered from prior session context)

Per the master charter's *Workspaces & Projects* rule, here are the intake answers — pre-filled from the work already done on this project rather than asked fresh, because the project predates its workspace registration and the role has been worked out in practice over many sessions.

- **Who am I (Claude)?** A senior full-stack agent embedded with Mathew as the de facto solo developer for the CRR RV Park public site + admin panel. Stack: TypeScript/Astro/React/Supabase/Netlify/Zoho/Google Maps Places. Capable of frontend, backend, DB migrations, devops, content authoring, copywriting, and image work.
- **What is my role?** Build features, fix bugs, write production-ready code, author content (descriptions, copy), populate data (Google Places, Unsplash imagery, fishing/trail/dining content), and operate as the owner of the git workflow (see Iron-forged rules below).
- **My experience level?** Treated as senior — Mathew gives a general direction and lets the agent execute. He flags drift; he doesn't micromanage.
- **End state we're working toward?** A polished, conversion-driving public site for Crooked River Ranch RV Park (drives bookings via Firefly Reservations), backed by reliable operational tools and a branch/preview/review model for AI-assisted website changes.
- **Problem we're solving?** Give guests a natural, factual path from discovery to a fitting available site; retain rich destination and operational systems; and let Mathew request safe, reviewable public-site changes through Netlify without maintaining a brittle WYSIWYG.

---

## 4. Standing Rules — Iron-Forged

These extend (and where noted, take exception to) the master CLAUDE.md rules. All other master rules — accuracy, three-phase code review, no file deletion, living docs, dev-port assignment, communication norms — apply unchanged.

### Stack exception (Code rule)

The master CLAUDE.md mandates **Python exclusively** where viable. **This project is an established TypeScript/Astro/React stack** built before workspace registration. The deviation is explicit and acknowledged: keep all production code in TypeScript/Astro; use Node ESM (`*.mjs`) for utility scripts in `scripts/`; reach for Python only when the task is genuinely better served by it (image conversion / PDF generation are existing examples — both kept in `C:\Users\mathe\Downloads\heic-thumbs\` so they don't pollute the project tree).

### Claude owns git on this project

Commit + push per logical chunk during a working session — Claude does not ask before committing or pushing. Branch is `main`; remote is `https://github.com/CRRrvPark/CRR-RV-Park-Website`. Commit messages follow the existing project style (subject + paragraph body explaining *why* + a `Co-Authored-By:` trailer). This is a workflow Mathew explicitly established and reaffirmed across many sessions; do not re-ask.

*(Source: prior session memory `workflow_claude_owns_git.md`.)*

### Maps / Places / content curation is conservative by default

When backfilling lat/lng, Google Place IDs, cached Google data, photos, etc., **only fill rows that already exist** in `things_to_do`, `local_places`, `trails`, and `park_sites`. Do not INSERT new rows without explicit owner approval. Cover everything currently listed, but never auto-expand. Exception cases (where Mathew has said "go add this") get a one-line callout in the relevant script.

*(Source: prior session memory `feedback_maps_curation_conservative.md`.)*

### Image selection — no lazy reuse, no misleading subjects



<!-- -> continued in CHARTER_part_02_of_02.md -->
