---
export const prerender = false;

import AdminBase from '@layouts/AdminBase.astro';
import { Settings as SettingsPanel } from '@components/react/Settings.tsx';
---
<AdminBase
  title="Settings"
  subtitle="Integrations, site configuration, and where to change things that aren't surfaced here."
  active="settings"
>
  <SettingsPanel client:only="react" />
</AdminBase>
